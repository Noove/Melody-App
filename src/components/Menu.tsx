import { LucideBrickWall } from "lucide-react";

const Menu = ({
  setSelected,
}: {
  setSelected: React.Dispatch<React.SetStateAction<null>>;
}) => {
  return (
    <div className="align-center flex justify-center">
      <h1>Menu</h1>
    </div>
  );
};

export default Menu;
